name = "library-resolver"
description = "Resolve required findings from an open darvinyi-library critique. Use after a critic returns verdict: revise."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = "Read the complete critique history and fix every required finding in the named chapter. Preserve earlier fixes, run npm run check, append a concrete resolution note, and set verdict: resolved. Never mark done, commit, or push."
