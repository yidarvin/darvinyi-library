verdict: revise

## Critique round 1 — 2026-07-26

### Required

1. **Correct the central definition of fragility in “The Model.”** The draft says
   fragility means that variation produces “a loss larger than the system can absorb.”
   That narrows fragility to ruin or terminal failure. The recorded Taleb evidence
   defines fragility locally as a concave response that loses from variability, which
   can occur well before a loss becomes unabsorbable. Because this sentence defines
   one third of the signature model, revise it to distinguish response to disorder
   from severity or survivability of a particular loss.

2. **Rebuild Figure 89.5 so it teaches the barbell rather than two unexplained
   magnitudes.** Two horizontal bars at `0.82` and `0.3` do not show the separation
   between a protected base and bounded, asymmetric experiments, nor what the strategy
   avoids between those poles. The values also imply a quantitative allocation that
   the prose and evidence do not establish. In addition, `Bars` places “survival and
   reserves” on one unwrapped line beginning near the right edge of its 380-unit
   viewBox, so the label is clipped. Use an in-vocabulary structure that encodes the
   stated separation and keeps every label readable in the phone-width figure.

3. **Make Figure 89.6 encode consequence alignment.** Its current
   decision → exposure → outcome → review loop is a generic learning cycle. It never
   shows whether the decision-maker bears the downside or passes it to another person,
   which is the section’s defining contrast. Rework the structure or choose a more
   fitting vocabulary form so the visual itself distinguishes aligned consequences
   from externalized consequences.

4. **Complete the registry record before approval.** The `antifragile` entry has no
   `tier`, `thesis`, `framework`, or `diagrams` fields. The authoring spec requires
   those fields for a done page, including an explicit record of the signature model
   and the diagram forms actually used.

5. **Add recorded support for the two material book-specific attributions that the
   present evidence set does not substantiate.** The draft explicitly calls the
   protected-base/experiments idea “Taleb’s barbell image” and presents consequence
   alignment as a core Antifragile idea. The two recorded Taleb papers support convex
   payoffs, optionality, bounded trials, and local nonlinear response, while the
   publisher page supports the central triad and the book’s broad domains; none of the
   three identifies the barbell strategy or the consequence-bearing claim. Record
   direct support for those attributions, or narrow/remove them so every material
   attribution can be re-derived from the evidence file without a new search.

### Advisory

None.
